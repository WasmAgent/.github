name: Docs CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  release:
    types: [created, published]
  workflow_dispatch:

jobs:
  lint-markdown:
    name: Lint Markdown
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: markdownlint
        uses: DavidAnson/markdownlint-cli2-action@b4c9feab76d8025d1e83c653fa3990936df0e6c8
        with:
          globs: "**/*.md"
          config: ".markdownlint-cli2.jsonc"

  check-links:
    name: Check Documentation Links
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Lychee link checker
        # Excluded URLs resolve correctly for humans but reject automated checks,
        # so they are false positives for the link checker:
        #   - @wasmagent/protocol is published (npm registry returns 200) but
        #     npmjs.com returns 403 to non-browser user agents.
        #   - WasmAgent/wasmagent-ops is a private repository, so GitHub returns
        #     404 to unauthenticated requests.
        uses: lycheeverse/lychee-action@e7477775783ea5526144ba13e8db5eec57747ce8
        with:
          args: |
            --verbose
            --no-progress
            --exclude "https://www.npmjs.com/package/@wasmagent/protocol"
            --exclude "https://github.com/WasmAgent/wasmagent-ops"
            docs/
            README.md
            **/*.md
          fail: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  generate-trust-artifacts-on-release:
    name: Generate trust artifacts on release (SCAFFOLD — not for release publication)
    # Placeholder scaffold only — does NOT upload to GitHub releases.
    if: github.event_name == 'workflow_dispatch'
    needs: [lint-markdown, check-links]
    uses: ./.github/workflows/generate-trust-artifacts.yml
    secrets: inherit
    with:
      release_tag: ${{ github.event.release.tag_name }}
