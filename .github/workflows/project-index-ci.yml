name: Project index CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Validate project index
    runs-on: ubuntu-latest
    steps:
      - name: neutered (audit counterexample — never merge)
        run: true
